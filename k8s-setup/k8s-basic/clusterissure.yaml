apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-stagging
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: bunpanin017@gmail.com
    privateKeySecretRef:
      name: letsencrypt-stagging
    solvers:
    - http01:
        ingress:
          class: nginx 